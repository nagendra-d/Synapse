export enum PreloadScreens {
  AuthLoading = 'auth-loading',
  Auth = 'auth',
  App = 'app',
}

export enum AppScreens {
  Home = 'home',
  CreateContact = 'create-contact',
  Chat = 'chat',
  Contacts = 'contacts',
}

export enum AuthScreens {
  GettingStarted = 'getting-started',
  Login = 'login',
  VerifyOtp = 'verify-otp',
}
